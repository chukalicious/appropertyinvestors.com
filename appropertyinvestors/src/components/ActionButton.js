import Button from "@mui/material/Button";

const ActionButton = () => {
  return (
    <Button sx={{ color: "secondary", display: "none" }} variant="contained">
      contact us
    </Button>
  );
};

export default ActionButton;
